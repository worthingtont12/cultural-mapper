CREATE TABLE city_primary(
created_at TEXT,
id BIGINT PRIMARY KEY,
source TEXT,
text TEXT,
text_lang TEXT,
user_id BIGINT,
user_location TEXT,
user_handle TEXT,
user_lang TEXT
);

CREATE TABLE city_secondary(
id BIGINT PRIMARY KEY,
long TEXT,
lat TEXT
);

CREATE TABLE user_desc(
id BIGINT PRIMARY KEY,
user_id BIGINT,
user_desc TEXT
);
