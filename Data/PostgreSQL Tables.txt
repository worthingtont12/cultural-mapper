CREATE TABLE city_primary(
created_at TEXT,
id BIGINT PRIMARY KEY,
source TEXT,
text TEXT,
text_lang TEXT,
user_id BIGINT,
user_location TEXT,
user_handle TEXT,
user_desc TEXT,
user_lang TEXT
);

CREATE TABLE city_secondary(
id BIGINT PRIMARY KEY,
long NUMERIC,
lat NUMERIC,
coordinates TEXT
);

CREATE TABLE city_tertiary(
id BIGINT PRIMARY KEY,
hashtags TEXT,
urls TEXT,
mentions TEXT,

);
